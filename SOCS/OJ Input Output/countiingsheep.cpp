#include <stdio.h>


int main(){
	
	int N, M;
	 
	 scanf("%d", &N);
	 scanf("%d", &M);
	 
	 if(M != 0 && N != 0){
	 	for(int N; N<=M; N++){
	 		printf("%d\n", N);
		 }
	 }
	return 0;
}
